#include "PersonalizationSettings.hpp"

// PersonalizationSettings is intentionally header-only so the existing explicit
// CMake source list does not need a separate object file for this integration.
